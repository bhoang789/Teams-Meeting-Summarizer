## Core Instructions

# Project Overview: We are building a Teams Meeting Summarizer tool and we're using TDD. We will be feeding in Acceptance Criteria in Gherkin Syntax. Try not to generate any more files unless you ask me and follow the TDD prompts given.

# TDD workflow rules: Always write a failing test before writing implementation code. Implement only what's needed to pass the current test—no extra features or speculative abstractions. Run the full test suite before considering a task complete.

## Project knowledge
- **Tech Stack:** Python - When generating code, make it human-readable, even if it's not the shortest length. 
- **File Structure:**
  - `src/` – Application source code
  - `docs/` – All documentation
  - `tests/` – Unit, Integration, and Playwright tests
  - `resources/` - Contains features that are to be implemented
# Reference the /features folder when in doubt for additional context